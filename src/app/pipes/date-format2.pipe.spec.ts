import { DateFormatPipe2 } from './date-format.pipe';

describe('DateFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new DateFormatPipe2();
    expect(pipe).toBeTruthy();
  });
});
